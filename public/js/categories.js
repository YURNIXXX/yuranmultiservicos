const box=document.querySelector('#allCategories');const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));function icon(n=''){n=n.toLowerCase();if(/eletr|energia/.test(n))return'bi-lightning-charge';if(/design|graf|branding/.test(n))return'bi-palette';if(/marketing/.test(n))return'bi-megaphone';if(/agro|jard|pecu/.test(n))return'bi-flower1';if(/inform|tecn|web|program/.test(n))return'bi-laptop';if(/constru|obra|carpin|sold/.test(n))return'bi-tools';if(/rh|recurso|admin|contab/.test(n))return'bi-briefcase';return'bi-grid'}fetch('/api/categories').then(r=>r.json()).then(rows=>{box.innerHTML=rows.length?rows.map(c=>`<a class="category-card" href="/profissionais?category=${encodeURIComponent(c.name)}"><i class="bi ${icon(c.name)}"></i><div><h2>${esc(c.name)}</h2><p>${c.count} serviço${c.count===1?'':'s'} publicado${c.count===1?'':'s'}</p></div><i class="bi bi-arrow-right"></i></a>`).join(''):'<div class="empty-state">Ainda não há categorias publicadas.</div>'}).catch(()=>box.innerHTML='<div class="empty-state">Não foi possível carregar as categorias.</div>');
