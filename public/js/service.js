let siteData,service,currentProject,galleryIndex=0;const $=(s,r=document)=>r.querySelector(s),$$=(s,r=document)=>[...r.querySelectorAll(s)];const esc=s=>String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));function images(p){return p.images?.length?p.images:(p.image?[p.image]:[])}function normalizeUrl(v=''){v=String(v).trim();if(!v||v==='#')return '#';return /^https?:\/\//i.test(v)?v:`https://${v.replace(/^\/+/, '')}`}
async function init(){const r=await fetch('/api/site');siteData=await r.json();const id=decodeURIComponent(location.pathname.split('/').filter(Boolean).pop()||'');service=siteData.services.find(s=>s.id===id);if(!service){document.body.innerHTML='<main class="section"><h1>Serviço não encontrado</h1><a href="/">Voltar ao site</a></main>';return}render();}
function iconMarkup(){return service.iconImage?`<img src="${esc(service.iconImage)}" alt="">`:`<i class="bi ${esc(service.iconClass||'bi-briefcase')}"></i>`}
function render(){const s=siteData.settings||{};document.documentElement.style.setProperty('--service-color',service.color||'#00a884');document.documentElement.style.setProperty('--nav-hover',service.color||s.navHoverColor||'#00a884');const siteName=s.siteName||'Yuran Multicerviços';$('#footerName').textContent=siteName;$('#footerCopyrightName').textContent=siteName;$('#footerTagline').textContent=s.tagline||'Conectamos necessidades a profissionais preparados para entregar resultados.';$('#serviceTitle').textContent=service.title;$('#serviceIntro').textContent=service.description||'';$('#serviceDetails').textContent=service.details||service.description||'';$('#serviceIcon').innerHTML=iconMarkup();document.title=`${service.title} | ${s.siteName||'Yuran Multicerviços'}`;const headerLogo=s.headerLogo||s.logo||'';if(headerLogo){$('#brandLogo').src=headerLogo;$('#brandLogo').hidden=false;$('#brandMark').hidden=true}else{$('#brandLogo').hidden=true;$('#brandMark').hidden=false}if(s.favicon)$('#faviconLink').href=s.favicon;if(service.banner){const b=$('#serviceBanner');b.classList.add('has-image');b.style.backgroundImage=`url("${service.banner.replace(/"/g,'%22')}")`}const find=`/profissionais?q=${encodeURIComponent(service.title)}`;$('#findProfessionalBtn').href=find;$('#findProfessionalBtnBottom').href=find;const contacts=[],address=s.address||s.location||'';if(s.phone)contacts.push(`<a href="tel:${esc(String(s.phone).replace(/[^+\d]/g,''))}"><i class="bi bi-telephone"></i><span>${esc(s.phone)}</span></a>`);if(s.email)contacts.push(`<a href="mailto:${esc(s.email)}"><i class="bi bi-envelope"></i><span>${esc(s.email)}</span></a>`);if(s.whatsapp){const wa=String(s.whatsapp).replace(/\D/g,'');contacts.push(`<a href="https://wa.me/${esc(wa)}" target="_blank" rel="noopener"><i class="bi bi-whatsapp"></i><span>WhatsApp</span></a>`)}if(address)contacts.push(`<span><i class="bi bi-geo-alt"></i><span>${esc(address)}</span></span>`);$('#footerContacts').innerHTML=contacts.join('');const socialMap={Instagram:'bi-instagram',Facebook:'bi-facebook',LinkedIn:'bi-linkedin',YouTube:'bi-youtube',TikTok:'bi-tiktok',X:'bi-twitter-x',Behance:'bi-behance',Dribbble:'bi-dribbble',WhatsApp:'bi-whatsapp',Telegram:'bi-telegram',Pinterest:'bi-pinterest',GitHub:'bi-github',Website:'bi-globe2'};$('#socialLinks').innerHTML=(siteData.socials||[]).filter(x=>x.url&&x.url!=='#').map(x=>`<a class="social-icon-link" href="${esc(normalizeUrl(x.url))}" target="_blank" rel="noopener" aria-label="${esc(x.platform||'Rede social')}"><i class="bi ${esc(x.iconClass||socialMap[x.platform]||'bi-link-45deg')}"></i></a>`).join('');const projects=siteData.portfolio.filter(p=>p.service===service.title);$('#servicePortfolio').innerHTML=projects.length?projects.map(p=>{const imgs=images(p);return`<article class="portfolio-card" data-project="${esc(p.id)}" tabindex="0">${imgs[0]?`<div class="portfolio-media-wrap"><img class="portfolio-media" src="${esc(imgs[0])}" alt="${esc(p.title)}">${imgs.length>1?`<span class="image-badge"><i class="bi bi-images"></i> ${imgs.length}</span>`:''}</div>`:''}<div class="portfolio-info"><h3>${esc(p.title)}</h3><p class="portfolio-excerpt">${esc(p.description||'')}</p><button class="project-view-btn" type="button">Ver projeto <i class="bi bi-arrow-right"></i></button></div></article>`}).join(''):'<div class="empty-state">Ainda não há trabalhos publicados para este serviço.</div>';$$('[data-project]').forEach(c=>{c.onclick=()=>openProject(c.dataset.project);c.onkeydown=e=>{if(e.key==='Enter')openProject(c.dataset.project)}});}
function openProject(id){currentProject=siteData.portfolio.find(p=>p.id===id);if(!currentProject)return;galleryIndex=0;$('#modalService').textContent=currentProject.service;$('#modalProjectTitle').textContent=currentProject.title;$('#modalProjectDescription').textContent=currentProject.description||'';updateGallery();$('#portfolioModal').showModal()}
function updateGallery(){const a=images(currentProject||{});if(!a.length)return;$('#galleryImage').src=a[galleryIndex];$('#galleryCount').textContent=`${galleryIndex+1} / ${a.length}`;$('#galleryPrev').hidden=$('#galleryNext').hidden=a.length<2}function move(d){const a=images(currentProject||{});if(!a.length)return;galleryIndex=(galleryIndex+d+a.length)%a.length;updateGallery()}$('#galleryPrev').onclick=()=>move(-1);$('#galleryNext').onclick=()=>move(1);$('#portfolioClose').onclick=()=>$('#portfolioModal').close();document.addEventListener('keydown',e=>{if($('#portfolioModal').open){if(e.key==='ArrowLeft')move(-1);if(e.key==='ArrowRight')move(1)}});const saved=localStorage.getItem('theme')||(matchMedia('(prefers-color-scheme:dark)').matches?'dark':'light');document.documentElement.dataset.theme=saved;$('#themeToggle').onclick=()=>{const n=document.documentElement.dataset.theme==='dark'?'light':'dark';document.documentElement.dataset.theme=n;localStorage.setItem('theme',n)};$('#menuToggle').onclick=()=>$('#nav').classList.toggle('open');$$('#nav a').forEach(a=>a.onclick=()=>$('#nav').classList.remove('open'));$('#year').textContent=new Date().getFullYear();init();
