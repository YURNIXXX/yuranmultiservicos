let siteData,service,currentProject,galleryIndex=0;const $=(s,r=document)=>r.querySelector(s),$$=(s,r=document)=>[...r.querySelectorAll(s)];const esc=s=>String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));function images(p){return p.images?.length?p.images:(p.image?[p.image]:[])}function normalizeUrl(v=''){v=String(v).trim();if(!v||v==='#')return '#';return /^https?:\/\//i.test(v)?v:`https://${v.replace(/^\/+/, '')}`}
async function init(){const r=await fetch('/api/site');siteData=await r.json();const id=decodeURIComponent(location.pathname.split('/').filter(Boolean).pop()||'');service=siteData.services.find(s=>s.id===id);if(!service){document.body.innerHTML='<main class="section"><h1>Serviço não encontrado</h1><a href="/">Voltar ao site</a></main>';return}render();}
function iconMarkup(){return service.iconImage?`<img src="${esc(service.iconImage)}" alt="">`:`<i class="bi ${esc(service.iconClass||'bi-briefcase')}"></i>`}
function render(){const s=siteData.settings||{};document.documentElement.style.setProperty('--service-color',service.color||'#00a884');document.documentElement.style.setProperty('--nav-hover',service.color||s.navHoverColor||'#00a884');$('#brandName').textContent=s.siteName||'MultiServiços';$('#footerName').textContent=s.siteName||'MultiServiços';$('#footerTagline').textContent=s.tagline||'';$('#serviceTitle').textContent=service.title;$('#serviceIntro').textContent=service.description||'';$('#serviceDetails').textContent=service.details||service.description||'';$('#serviceIcon').innerHTML=iconMarkup();$('#serviceFormName').value=service.title;document.title=`${service.title} | ${s.siteName||'MultiServiços'}`;if(s.logo){$('#brandLogo').src=s.logo;$('#brandLogo').hidden=false;$('#brandMark').hidden=true}if(s.favicon)$('#faviconLink').href=s.favicon;if(service.banner){const b=$('#serviceBanner');b.classList.add('has-image');b.style.backgroundImage=`url("${service.banner.replace(/"/g,'%22')}")`}$('#contactList').innerHTML=`<div><strong>Telefone</strong><br>${esc(s.phone||'')}</div><div><strong>E-mail</strong><br>${esc(s.email||'')}</div><div><strong>Localização</strong><br>${esc(s.location||'')}</div>`;const projects=siteData.portfolio.filter(p=>p.service===service.title);$('#servicePortfolio').innerHTML=projects.length?projects.map(p=>{const imgs=images(p);return`<article class="portfolio-card" data-project="${esc(p.id)}" tabindex="0">${imgs[0]?`<div class="portfolio-media-wrap"><img class="portfolio-media" src="${esc(imgs[0])}" alt="${esc(p.title)}">${imgs.length>1?`<span class="image-badge"><i class="bi bi-images"></i> ${imgs.length}</span>`:''}</div>`:''}<div class="portfolio-info"><h3>${esc(p.title)}</h3><p class="portfolio-excerpt">${esc(p.description||'')}</p><span class="open-project">Ver detalhes <i class="bi bi-arrows-angle-expand"></i></span></div></article>`}).join(''):'<div class="empty-state">Ainda não há trabalhos publicados para este serviço.</div>';$$('[data-project]').forEach(c=>{c.onclick=()=>openProject(c.dataset.project);c.onkeydown=e=>{if(e.key==='Enter')openProject(c.dataset.project)}});}
function openProject(id){currentProject=siteData.portfolio.find(p=>p.id===id);if(!currentProject)return;galleryIndex=0;$('#modalService').textContent=currentProject.service;$('#modalProjectTitle').textContent=currentProject.title;$('#modalProjectDescription').textContent=currentProject.description||'';const l=$('#modalProjectLink');if(currentProject.link&&currentProject.link!=='#'){l.href=normalizeUrl(currentProject.link);l.hidden=false}else l.hidden=true;updateGallery();$('#portfolioModal').showModal()}
function updateGallery(){const a=images(currentProject||{});if(!a.length)return;$('#galleryImage').src=a[galleryIndex];$('#galleryCount').textContent=`${galleryIndex+1} / ${a.length}`;$('#galleryPrev').hidden=$('#galleryNext').hidden=a.length<2}function move(d){const a=images(currentProject||{});if(!a.length)return;galleryIndex=(galleryIndex+d+a.length)%a.length;updateGallery()}$('#galleryPrev').onclick=()=>move(-1);$('#galleryNext').onclick=()=>move(1);$('#portfolioClose').onclick=()=>$('#portfolioModal').close();document.addEventListener('keydown',e=>{if($('#portfolioModal').open){if(e.key==='ArrowLeft')move(-1);if(e.key==='ArrowRight')move(1)}});const saved=localStorage.getItem('theme')||(matchMedia('(prefers-color-scheme:dark)').matches?'dark':'light');document.documentElement.dataset.theme=saved;$('#themeToggle').onclick=()=>{const n=document.documentElement.dataset.theme==='dark'?'light':'dark';document.documentElement.dataset.theme=n;localStorage.setItem('theme',n)};$('#menuToggle').onclick=()=>$('#nav').classList.toggle('open');$$('#nav a').forEach(a=>a.onclick=()=>$('#nav').classList.remove('open'));$('#contactForm').onsubmit=e=>{e.preventDefault();const v=Object.fromEntries(new FormData(e.currentTarget)),s=siteData.settings;const text=`Olá! Gostaria de solicitar um serviço.%0A%0A*Nome:* ${encodeURIComponent(v.nome)}%0A*Telefone:* ${encodeURIComponent(v.telefone)}%0A*Serviço:* ${encodeURIComponent(v.servico)}%0A*Detalhes:* ${encodeURIComponent(v.mensagem||'')}`;window.open(`https://wa.me/${String(s.whatsapp).replace(/\D/g,'')}?text=${text}`,'_blank','noopener')};$('#year').textContent=new Date().getFullYear();init();
